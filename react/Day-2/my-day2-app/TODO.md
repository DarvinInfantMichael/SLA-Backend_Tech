# TODO - Fix invalid hook call / routing

- [ ] Add missing dependency: `react-router-dom` to `my-day2-app/package.json`.
- [ ] Update `src/main.jsx` to use `AppRoutes` (not direct page rendering) and keep `BrowserRouter`.
- [ ] Implement `src/routes/AppRoutes.jsx` using v6 `Routes`/`Route` and page components.
- [ ] Update `src/components/Navbar.jsx` Links to use proper `to` paths for each route.
- [ ] Run `npm install` and `npm run dev` (or `npm run build`) to verify error is resolved.

